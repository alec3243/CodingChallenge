# CameraIQ Coding Challenge

### Requirements
* Java 11

### How to build and run in terminal
* Navigate to project root directory
* `./gradlew --refresh-dependencies bootRun`

### How to build and run in IntelliJ
* Sync gradle
* Run `MainApplication`